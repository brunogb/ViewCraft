/// Placeholder — no components have been promoted here yet. Everything still
/// lives in `UnpublishedViewCraft`, the local package bundled with the
/// ViewCrafter app, until it's ready to ship with real versioning.
public enum ViewCraft {}
